parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            elasticsearch:
              source: git
              address: 'https://github.com/tcpcloud/salt-formula-elasticsearch.git'
              revision: ${_param:salt_formula_branch}
            heka:
              source: git
              address: 'https://github.com/tcpcloud/salt-formula-heka.git'
              revision: ${_param:salt_formula_branch}
            kibana:
              source: git
              address: 'https://github.com/tcpcloud/salt-formula-kibana.git'
              revision: ${_param:salt_formula_branch}
            java:
              source: git
              address: 'https://github.com/tcpcloud/salt-formula-java.git'
              revision: ${_param:salt_formula_branch}
